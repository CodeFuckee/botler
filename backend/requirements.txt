fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
python-multipart>=0.0.9
PyYAML>=6.0
APScheduler>=3.10,<4
python-dotenv>=1.0
pytest>=8.0
pytest-cov>=6.0
# Prometheus 指标端点（issue #208）：/metrics 文本格式，任务状态/执行时长/webhook/GitLab API/队列/磁盘/DB 观测（prometheus-client 为纯 Python 零依赖）
prometheus-client>=0.20

# 并行执行（issue #211）：pytest-xdist 提供 -n auto / -n 4 多进程分片，
# 加速 CI backend:test 与本地全量测试（各用例使用独立 tmp_path SQLite 临时库，
# 无共享状态，可安全并行）
pytest-xdist>=3.0
# 代码质量门禁（issue #203）：ruff check botler tests（规则见 ruff.toml，仅 F 组）
ruff>=0.9
minio>=7.2
# Web 终端（issue #183）：terminado 提供标准 WebSocket 终端服务，Tornado 承载
tornado>=6.4
terminado>=0.18

# dsh 会话根目录压缩编码归一化（issue #302）：runtime 默认 zstd 压缩，
# 遗留明文 session.jsonl 需转成 zstd 分帧格式（zstandard 提供压缩）
zstandard>=0.22
# 图片压缩（issue #310）：sync-logo 上传 GitLab 项目头像前，用 Pillow 把
# 超过 200KB 上限的 logo 压缩到 200KB 以内（JPEG/WebP 降质 + 转码 + 降分辨率）
Pillow>=10.0
